import React from "react";

export const SaveButton = e => {
  return (
    <div>
      <button onClick={() => this.saveTunes(e)} style={{ margin: "3%" }}>
        Save
      </button>
    </div>
  );
};
