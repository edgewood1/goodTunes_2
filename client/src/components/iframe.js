import React from "react";

const Iframe = props => (
  <div>
    <Iframe
      src={
        "https://open.spotify.com/embed?uri=spotify:track:" +
        this.props.playerID
      }
      title="x"
      frameBorder="0"
      width="100%"
    />
  </div>
);

export default Iframe;
